# Gradle / Android
.gradle/
build/
plugins/*/build/
local.properties
.kotlin/

# JS
node_modules/
package-lock.json
yarn.lock
*.tsbuildinfo

# Release planning scratch (the CI scripts write these; harmless locally)
published/
releases.json
plan.tsv

# Output of the old build.mjs pipeline, which this repo no longer uses
dist/

# Misc
.DS_Store
.idea/
*.iml
collected.log
README.original.md

# Local-only, work in progress -- deliberately not published.
# A local `./gradlew packageAllPlugins` still builds it, but releases run in CI from a clean
# checkout, so it can never enter the pool or index.json.
plugins/jump-to-top/

# Local-only at the original author's request -- must not be published.
# Same mechanics as above: local builds see it, the CI release never does.
plugins/multi-scrobbler/

# Local-only, not ready to publish.
plugins/who-reacted/

# Planning notes, kept local.
docs/plugin-ideas.md
