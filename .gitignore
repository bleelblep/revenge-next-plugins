node_modules/
dist/
yarn.lock
package-lock.json
collected.log
*.tsbuildinfo
README.original.md

# Local-only, work in progress -- deliberately not published.
# build.mjs still picks it up for local `dist/` builds and serve.mjs, but CI builds from a clean
# checkout, so it never reaches gh-pages or index.json.
plugins/jump-to-top/

# Local-only at the original author's request -- must not be published.
# Same mechanics as above: local `dist/` builds and serve.mjs still see it, CI does not.
plugins/multi-scrobbler/

# Local-only, not ready to publish.
# Local builds still include it, but clean CI checkouts do not.
plugins/who-reacted/

# Planning notes, kept local.
docs/plugin-ideas.md
